import React from "react";
import { View } from "react-native";

// This is just a placeholder component for the add tab
// The actual navigation happens in _layout.tsx through the tabPress listener
const Add = () => {
  return <View />;
};

export default Add;
